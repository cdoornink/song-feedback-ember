<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="brand" href="#">Song Feedback</a>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <li>{{#linkTo songs}} Browse {{/linkTo}}</li>
          {{#if SF.loginController.content.firstName}}
          <li>{{#linkTo my-songs}}My Feedback {{/linkTo}}</li>
          {{/if}}
        </ul>
        <ul class="nav  pull-right">
          {{#if SF.loginController.content.firstName}}
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              {{SF.loginController.content.firstName}} {{SF.loginController.content.lastName}}
                <b class="caret" style="color:white"></b>
              </a>
              <ul class="dropdown-menu">
                <li><a href="#">Settings</a></li>
                <li><a href="#">Upload Song</a></li>
                <li>{{#linkTo logout}}Sign out{{/linkTo}}</li>
              </ul>
            </li>
          {{else}}
            <li>{{#linkTo login}}Sign In{{/linkTo}}</li>
          {{/if}}
        </ul>
      </div>
    </div>
  </div>
</div>
<div id="main">{{outlet}}</div>
<div id="player">
<div class="rating-container">
<div class="step10"></div>
<div class="step9"></div>
<div class="step8"></div>
<div class="step7"></div>
<div class="step6"></div>
<div class="step5"></div>
<div class="step4"></div>
<div class="step3"></div>
<div class="step2"></div>
<div class="step1"></div>
</div>
</div>