<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="brand" href="#">Song Feedback</a>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <li>{{#link-to "songs"}} Browse {{/link-to}}</li>
          {{#if SF.loginController.content.firstName}}
          <li>{{#link-to "my-songs"}}My Feedback {{/link-to}}</li>
          {{/if}}
        </ul>
        <ul class="nav  pull-right">
          {{#if SF.loginController.content.firstName}}
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              {{SF.loginController.content.firstName}} {{SF.loginController.content.lastName}}
                <b class="caret" style="color:white"></b>
              </a>
              <ul class="dropdown-menu">
                <li>{{#link-to "upload"}}Upload Song{{/link-to}}</li>
                <li>{{#link-to "logout"}}Sign out{{/link-to}}</li>
              </ul>
            </li>
          {{else}}
            <li>{{#link-to "login"}}Sign In{{/link-to}}</li>
          {{/if}}
        </ul>
      </div>
    </div>
  </div>
</div>
<div class="main">{{outlet}}</div>
{{view SF.PlayerView}}