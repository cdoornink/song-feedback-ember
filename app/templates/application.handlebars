<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="brand" href="#">Song Feedback</a>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <li>{{#link-to "songs"}} Songs {{/link-to}}</li>
          {{#if SF.loginController.content.firstName}}
          <li>{{#link-to "my-songs"}}My Feedback {{/link-to}}</li>
          {{/if}}
        </ul>
        <ul class="nav  pull-right">
          {{#if SF.loginController.content.firstName}}
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              {{SF.loginController.content.firstName}} {{SF.loginController.content.lastName}}
                <b class="caret" style="color:white"></b>
              </a>
              <ul class="dropdown-menu">
                <li>{{#link-to "upload"}}Upload Song{{/link-to}}</li>
                <li>{{#link-to "logout"}}Sign out{{/link-to}}</li>
              </ul>
            </li>
          {{else}}
            <li>{{#link-to "login"}}Sign In{{/link-to}}</li>
          {{/if}}
        </ul>
      </div>
    </div>
  </div>
</div>
<div class="main">{{outlet}}</div>
{{view SF.PlayerView}}

{{#view SF.UploadConfirmationView contentBinding="SF.UploadConfirmationController"}}
  <div class="modal">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-hidden="true" {{action 'closeUploadConfirmation'}}>&times;</button>
      <h3>Upload Completed!</h3>
    </div>
    <div class="modal-body">
      <p>Thanks for uploading "{{view.content.name}}" to Song Feedback!</p>
      <p>Question: What's better than unbiased feedback?<br/>
          Answer: Immediate unbiased feedback! 
      </p>
        <p>You might have noticed that this site isn't swarming with users yet. Do yourself a favor and let your social circles know that you've just posted a song by clicking one or three of these share icons so you can start getting some feedback right away!
      </p>
      {{share-container item=view.content}}
    </div>
    <div class="modal-footer">
      <a href="#" class="btn" {{action 'closeUploadConfirmation'}}>Close</a>
    </div>
  </div>
{{/view}}
  